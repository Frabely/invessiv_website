/** The assignee facet value that means "the member looking at the list", resolved on the server. */
export const TASK_LIST_ASSIGNEE_ME = "me";

/** Rows per page of the task overview. */
export const TASK_LIST_PAGE_SIZE = 25;
